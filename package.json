{
  "name": "hexagonal-webmcp",
  "private": true,
  "type": "module",
  "description": "Hexagonal WebMCP: the agent declares its ports as a WebMCP spec; each host implements the adapters.",
  "scripts": {
    "start": "node start.js",
    "agent": "node agent-server/server.js",
    "solestack": "node consumer-solestack/server.js",
    "norvik": "node consumer-norvik/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
